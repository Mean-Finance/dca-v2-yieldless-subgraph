specVersion: 0.0.3
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum/contract
    name: Factory
    network: {{network}}
    source:
      startBlock: {{factoryStartBlock}}
      address: '{{factory}}'
      abi: Factory
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      file: ./src/mappings/factory.ts
      entities:
        - Factory
        - Pair
        - Transaction
        - Token
      abis:
        - name: Factory
          file: ./abis/Factory.json
        - name: Pair
          file: ./abis/Pair.json
        - name: ERC20
          file: ./abis/ERC20Detailed.json
      eventHandlers:
        - event: PairCreated(indexed address,indexed address,address)
          handler: handlePairCreated
  - kind: ethereum/contract
    name: GlobalParameters
    network: {{network}}
    source:
      startBlock: {{globalParametersStartBlock}}
      address: '{{globalParameters}}'
      abi: GlobalParameters
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      file: ./src/mappings/global-parameters.ts
      entities:
        - GlobalParameters
        - SwapInterval
        - Transaction
      abis:
        - name: GlobalParameters
          file: ./abis/GlobalParameters.json
      eventHandlers:
        - event: SwapIntervalsAllowed(uint32[],string[])
          handler: handleSwapIntervalsAllowed
templates:
  - kind: ethereum/contract
    name: Pair
    network: {{network}}
    source:
      abi: Pair
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      file: ./src/mappings/pair.ts
      entities:
        - Pair
        - Position
        - PositionState
        - PairSwap
        - SwapInterval
        - PairSwapInterval
        - PositionPairSwap
        - Transaction
        - Token
      abis:
        - name: Pair
          file: ./abis/Pair.json
      eventHandlers:
        - event: Deposited(indexed address,uint256,address,uint160,uint32,uint32,uint32)
          handler: handleDeposited
        - event: Modified(indexed address,uint256,uint160,uint32,uint32)
          handler: handleModified
        - event: Terminated(indexed address,uint256,uint256,uint256)
          handler: handleTerminated
        - event: Withdrew(indexed address,uint256,address,uint256)
          handler: handleWithdrew
        - event: WithdrewMany(indexed address,uint256[],uint256,uint256)
          handler: handleWithdrewMany
        - event: Swapped(indexed address,indexed address,uint256,uint256,uint32,((uint32,uint32,uint256,uint256)[],uint8,uint256,uint256,uint256,uint256,uint256,uint256,uint256,uint256,address,address))
          handler: handleSwapped
        - event: Transfer(indexed address,indexed address,indexed uint256)
          handler: handlePositionTransfer